{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Watch run Api",
            "type": "shell",
            "command": "dotnet",
            "args": [
                "watch",
                "run",
                "--no-restore",
                "--no-hot-reload"
            ],
            "options": {
                "cwd": "${workspaceFolder}/src/Api"
            },
            "problemMatcher": [

            ]
        }
    ]
}